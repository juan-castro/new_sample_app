%h1
  Edit user

= form_for(@user) do |f|

  = render 'form', :f => f

  .actions
    = f.submit "Update User"

  %div
    = gravatar_for @user
    %a{:href => "http://gravatar.com/emails", :target => "_blank" } change
