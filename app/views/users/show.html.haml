%table.profile
  %tr
    %td.main
      %h1
        #{ gravatar_for @user }
        #{ @user.name }

    %td.sidebar.round
      %strong
        name
      = @user.name
      %br/

      %strong
        URL
      = link_to user_path(@user), @user
      %br/
